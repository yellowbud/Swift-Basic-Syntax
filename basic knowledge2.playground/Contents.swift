import UIKit

/*
 
 断点调试的基本流程如下：
 单击断点的行号，设置断点。   断点消失：按住断点拖向旁边
 
 show the report navigator:日志导航面板，此面板将会显示在开发整个项目的过程中发生的所有操作
 
 格式化代码：editor -> structure -> Re-indent
 
 如果要缩进多行，则需要使用快捷键，其中 Command+]表示缩进，Command+[表示反向缩进
 
     Ctrl + . 自动补全
 使用 Tab 键确认方法中的内容，或者通过 Ctrl+ / 快捷键在方法的参数中来回切换
 
 快速定位到代码行 Command+L ，输入行号并按 Return 键后
 
 shift Command Y 快捷键调出调试窗口:
 Step Over 按钮，将执行当前方法内的下一个语句。
 Step Into 按钮，如果当前语句表示方法调用，将单步执行当前方法调用内的第一行。
 Step Out 按钮，将跳出当前语句所在方法，到方法外的第一行
 */

